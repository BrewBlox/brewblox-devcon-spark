syntax = "proto3";

import "brewblox.proto";
import "nanopb.proto";

package blox;

message SetpointSimple {
  option (brewblox_msg).objtype = SetpointSimple;
  sint32 setting = 1 [ (brewblox).unit = Temp, (brewblox).scale = 4096, (nanopb).int_size = IS_32 ];
  bool enabled = 2;
}
